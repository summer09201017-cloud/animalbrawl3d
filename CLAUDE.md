# CLAUDE.md — 毛毛大亂鬥(animalbrawl3d)給接手的 AI 看

## 這是什麼

3D **物理布娃娃**派對遊戲(Party Animals 型):可愛動物互推,推下浮空平台就得分。
同機兩人或對電腦。**HFPC 艦隊第一個用真物理引擎的專案**。

- 主題:**純娛樂**(使用者 0828 拍板)—— 不接經文、不綁聖經故事,進「街機合輯」分類
- 技術:Vite + three + **@dimforge/rapier3d-compat**,零後端、離線可玩、PWA

## 現況(2026-09-01・v1.3,已上線、已進大廳)

- `node test/all.mjs` → **119 過 / 0 失敗 🟢**(physics 50 / rules 47 / smoke 22)
- `npm run verify` → **52 過 / 0 失敗 🟢**(🔬 全套,約 155 秒;真 Chromium + 真 WebGL,截圖存 `.shots/`;
  本機與線上同一支跑)
- `npm run verify:quick` → **30 過 / 0 失敗 🟢**(⚡ 快檢,約 21 秒;日常改一行時跑這個。
  跳過四視角逐幀方向量測與「走過去把人打到」;**改鏡頭/構圖/輸入後一定要跑全套**)
- `npm run build` 過:`index` 510 KB(gzip 133)+ `rapier.es` 2.06 MB(**gzip 762 KB**)
- 線上:CF Pages `hfpc-animalbrawl3d` → https://hfpc-animalbrawl3d.pages.dev
- 大廳:總入口「經典街機合輯」第 5 張卡(`hfpc-bible-games` 的 `src/data.js`,新 kind `brawl`)
- 統計:三層打點都鋪好(開啟 / `-done` 完賽 / `-dwell` 真實停留)

### 0901 下午那一輪改了什麼(使用者點名的三件觀感 + 大廳)

| 使用者說 | 真因 | 做法 | 可驗的斷言 |
|---|---|---|---|
| 固定機位上半部一大片空天空、角色偏小 | 距離 7.0/俯角 4.2 太淺太遠 | 拉近到 5.0、俯角加大到 4.8、看的點下移到胸口與台面之間 | 角色佔畫面高 13% → **15~19%**(固定機位門檻 14%,替代機位 12%) |
| 文件說有雲、畫面一朵都沒有 | 三層疊:材質吃霧而**霧色跟天空同色**、擺在 y=7~19(俯角看不到)、散太遠 | `fog:false` + 大部分移到**台子下方**(浮空台本來就該看到腳下的雲)+ 環狀分佈 | 框內雲數 0 → **11~13 朵**(非俯瞰視角 ≥2) |
| 跟隨視角太低太近、常看不到對手 | 6.2 遠 / 3.4 高,且不跟著兩隻的間距退 | 拉高拉遠 + 跟著間距退 | 兩隻都在框內(含邊界留白)且角色 ≥12% |

順帶:**高空俯瞰改成固定在場地中心正上方**、高度由半徑與實際 fov 算出 ——
原本它跟著玩家平移,玩家偏離中心 1.5 m 遠端台緣就出框,而那個視角的職責正是「看到整個台子」。

### 0901 真瀏覽器驗收抓到、headless 抓不到的兩個(都已修)

| 症狀 | 病根 | 為什麼 headless 全綠 |
|---|---|---|
| **HUD 整組不顯示**(比分/回合訊息/視角提示全都看不到) | `#hud` 在 CSS 是 `display:none`,`main.js` 把 display 指派成**空字串**想「恢復顯示」⇒ 清掉 inline 樣式後回落到 CSS 的 none | 測試讀的是 `game.hud()` 那個**物件**,它永遠是對的;連 `getComputedStyle` 拿字級也拿得到(祖先 `display:none` 不影響 font-size)⇒ **假綠燈** |
| **比賽結束後「再來一場」鈕不出現**(高 0px) | 同一個病根(`#again` 也是 CSS `display:none`) | 同上。而且平板沒有實體鍵盤按不到 `R` ⇒ **一場打完就卡死** |

⇒ 已補**靜態守門**(`test/smoke.mjs`):CSS 裡藏起來的 id,JS 不准用空字串去顯示它。
   全域也有同族 hook `css-hidden-toggle-guard`(#25,尋羊記血案),但那支只在「AI 動到檔案」時響;
   repo 內的測試每次 `npm test` 都跑,兩層互補。

## 為什麼選 rapier3d-**compat**(不是 rapier3d,也不是 cannon-es)

- 使用者點名要**物理引擎**,而全艦隊在此之前**零物理引擎**
  (掃到的 4 筆 `rapier` 全是「西洋劍」的英文名,誤判)
- `-compat` 把 wasm 內嵌成 base64 ⇒ **不需要任何 wasm 外掛或 vite 特殊設定**,
  少一層建置設定、少一個「本機好但線上壞」的風險面
- ★★ 而且它**在 node 跑得起來**(實測:球從 y=5 落到 y=0.075)⇒
  只要物理層不 import three,headless 測試就能驅動**真的物理**跑完一局。
  這是本專案能躲掉 3d-game-kit 那條「沒有 renderer 就 early-return ⇒ 測試永遠綠」的原因。
  ⇒ **不要把 three import 進 `src/physics.js`**,那條分界線一破,40 項物理測試當場失去意義。
- 代價:rapier 那個 chunk 2 MB(gzip 762 KB)。教室 Wi-Fi 第一次載得等一下,之後 SW 快取。

## 建構期間真的踩過的坑(每一條都有對應的測試)

| # | 坑 | 症狀 | 為什麼難查 | 測試 |
|---|---|---|---|---|
| 1 | Rapier 的 `capsule(halfHeight, radius)` **長軸是 Y**,我把手臂當 X 軸橫擺 | 錨點落在膠囊側面 ⇒ 每個肩關節都是長槓桿,兩幀炸到 `pelvis y=-385140` | 看起來像關節設定壞掉 | A1~A5 |
| 2 | 平衡力矩 `cross(up,(0,1,0))` **符號寫反** | 力矩把角色往正在倒的方向再推 = 正回饋 | 不拋錯、不進 console,只是「一出生就飛走」 | A2 A4 |
| 3 | 質量只有 **0.3 kg** | 力矩相對慣量大到必炸;理智夾一路夾 **8613 次** 才勉強沒飛走 | **「看起來能跑」的假象** —— 所以理智夾要可計數 | A1 **B1** |
| 4 | `grounded` 用「腳的 y 很低」判 | 掉出台外往虛空掉時條件照樣成立 ⇒ **腿彈簧在半空中把自己推回來**,飛到水平 59 m 掉台判定一次都沒觸發 | 症狀是「掉台判定壞了」,壞的其實是著地判準 | **D1** D3 |
| 5 | 射線起點在自己的小腿膠囊**內部** | `solid=true` 第一個命中就是自己(toi=0)⇒ 永遠判成沒著地,垮到 y=0.166 | — | C4 |
| 6 | 揮拳給**一次性衝量** | 拳頭離胸口最遠只有 0.304 m,而且**最遠在第 0 幀**(衝量瞬間被肩關節吃光)⇒ 要貼著才有拳 | 「打不到」很容易被當成手感問題去調數值 | **E2** E3 E6 |
| 7 | 抓取拿**拳頭位置**當原點 | 手垂在身側,眼前 0.85 m 的對手實際距離 ~1.0 m ⇒ 永遠抓不到 | 畫面上明明就在眼前 | **F1** F7 |
| 8 | 腳底煞車沒設速度上限 | 站超穩,但 **60 N·s 的側推推不下台** = 玩法壞掉 | **站樁測試全綠**,壞的是玩法不是畫面 | **G2** G1 |
| 9 | 抬頭彈簧只推頭、**沒有反作用力** | 對系統注入動量 ⇒ 角色被自己的脖子推著跑,站 6 秒漂 2.69 m | 症狀是「站不住」,差點去加大煞車(會把玩法一起殺掉) | A3 G1 |
| 10 | 脖子用**球窩**關節 | Rapier 的球窩**沒有角度限位** ⇒ 頭可以盪到胸口旁邊/下面,而那個姿勢**完全滿足關節約束** | 加力矩(只改朝向)、加位置彈簧、補反作用力、6 組參數掃描 —— **每組都有一項紅**,因為問題不在參數。改成帶限位的 revolute 後**一次修掉兩項紅** | A5 |
| 11 | AI 偶爾自己走下台 | 12 次跑測試紅 1 次 | **flaky 測試比沒測試更糟**:紅一次沒人查、綠一次就以為好了 | E2(+ 測試用固定種子 LCG) |
| 12 | `sw.js`/`icon.svg`/`manifest` 放在 **repo 根目錄** | vite 只複製 `public/` ⇒ 三個檔都沒進 dist ⇒ **上線後 SW 與圖示 404**、裝不到主畫面、離線打不開 | 本機 dev 一切正常(dev server 服務根目錄),首頁看起來也正常 | smoke 的 PWA 段 |

| 13 | CSS 藏起來的元素,JS 把 `display` 指派成**空字串**想顯示它 | HUD 整組隱形、結算鈕高 0px | **測試全綠**:資料物件永遠對,錯的是「看不看得見」 | smoke 最後一段(靜態) |
| 14 | 驗收腳本自己把角色走下台 / 門檻對錯了對象 | 12 條紅燈裡有 6 條是假的(跳不起來、打不到人、框不住) | 假紅**長得跟真 bug 一模一樣**:第一輪我差點去改產品。真兇是「台子半徑 3.6,而我每個視角按 D 走 1.5~1.8 m」與「站姿髖部 0.64,而我判活著的門檻寫 `y > -1`」 | 驗收腳本內的 `ensureAlive`/`measureForward` |

★★ 這張表最值得記住的一條:**#10**。我在那裡連續做了四次「調參數/加一層力」,
每一次都讓另一項變紅 —— 那是「問題不在參數」的訊號。改結構之後一次全綠。
⇒ **同一個地方調第三次還過不了,就該懷疑結構,不要繼續轉旋鈕。**

## 量值(改之前先讀)

`src/physics.js` 的 `TUNE`。全部按「一隻約 **8.9 公斤**的幼獸」算過 —— 改質量就要重算力道。
- `legK/legD` 腿彈簧(把身體撐到站姿高度;**只准往上推,不准往下拉**)
- `kneeStiff/kneeDamp` 膝馬達(太軟走路會蹲成爬行:實測 34 時走完只剩 0.387,站姿是 0.690)
- `brake/brakeMaxV` 腳底抓地(**只在低速作用**,不然推不下台)
- `neckK/neckD/neckLift/neckLiftD` 扶頭(**力與力矩都要成對施回胸口**)
- `punchImpulse` 是**持續施力(N)**,不是一次性衝量
- 尺寸 `SIZE` 刻意**矮胖**(頭大身短腿短)—— 與 3d-figure-kit 的「長腿 v2」相反,那條是給聖經人物/運動員的

## 本機地雷

1. **不要把 three import 進 `src/physics.js`**(理由見上面)
2. PWA 殼層檔一律放 `public/`(坑 #12)
3. 改了會上線的檔要 bump `public/sw.js` 的 `CACHE`
4. dev 不註冊 SW(`main.js` 已守 localhost)—— 不守會讓每次改動「慢一版」,測試誤判半天
5. 測試裡的 AI 一律傳固定種子的 `rng`,不要用 `Math.random`(坑 #11)

## 還沒做(roadmap.md 有完整清單)

- ✅ 真瀏覽器玩一輪 + 截圖驗收(0901;`npm run verify`,需先 `npm run dev`)
- ✅ 建 GitHub repo + 部署 CF Pages(0901)
- ✅ 返回大廳鈕 + 進大廳「經典街機合輯」卡片(0901)
- ✅ 使用統計三層打點(0901)
- 🟠 **下一件 = roadmap 第 1 列:3~4 人同場**(物理層的 `addAnimal` 本來就吃 N 隻;
  要改的是規則層誰得分、HUD 版面、出生點分佈、鏡頭要框得住所有人)
- 🟠 場地機關(會塌的邊緣、移動平台、彈跳墊)—— 這型遊戲的耐玩度幾乎全在關卡
- 🟠 語音播報(一律走 [[baked-voice-commentary]] 烤 mp3,**不可以**用瀏覽器內建語音)
