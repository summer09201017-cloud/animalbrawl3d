毛毛大亂鬥(animalbrawl3d)— 交接說明
產出日期:2026-08-28(建構)/ 2026-09-01 更新(真瀏覽器驗收 + 上線)

═══════════════════════════════════════════════════════════════
★ 接手第一件事
═══════════════════════════════════════════════════════════════
  1. cd Downloads/hfpc-git/animalbrawl3d && npm install
  2. node test/all.mjs   → 應該是 119 過 / 0 失敗 🟢(約 1 分鐘)
  3. npm run dev,另一個視窗:
       npm run verify:quick  → ⚡ 約 21 秒,應該 30 過 / 0 失敗(日常用這個)
       npm run verify        → 🔬 約 155 秒,應該 52 過 / 0 失敗(上線前、改鏡頭或輸入後用這個)
  4. 讀 CLAUDE.md 的「建構期間真的踩過的坑」那張表(14 條,每條都有對應測試)
  5. 讀 roadmap.md 的「🔜 待做」第 1 列 —— 那是下一步

═══════════════════════════════════════════════════════════════
★ 這是什麼
═══════════════════════════════════════════════════════════════
  3D 物理布娃娃派對遊戲(Party Animals 型)。四種可愛動物互推,推下浮空平台得分。
  同機兩人 or 對電腦。主題是**純娛樂**(使用者 0828 拍板,不接經文)。
  ★★ HFPC 艦隊**第一個用真物理引擎**的專案(此前全艦隊零物理引擎;
     掃到的 4 筆 rapier 全是「西洋劍」的英文名,誤判)。

═══════════════════════════════════════════════════════════════
★ 最重要的一條架構規矩
═══════════════════════════════════════════════════════════════
  **src/physics.js 不准 import three。**
  Rapier 的 compat 版在 node 跑得起來,所以只要物理層不碰 three/DOM,
  headless 測試就能驅動**真的物理引擎**跑完整一局 ——
  布娃娃站不站得起來、推不推得下台、拳頭到不到得了,全部是可以斷言的數字。
  ⇒ 那條分界線一破,50 項物理測試當場失去意義,而且不會有任何東西提醒你。
  ⇒ 0901 起這條**有測試守著了**(test/smoke.mjs 開頭那段,含「判準有效性」保險絲)。
     在此之前它只寫在註解裡 —— 註解攔不住人。
     ★ 這條心法已抽成跨專案 skill:physics-headless-boundary。

═══════════════════════════════════════════════════════════════
★ 現況(v1.3・已上線、已進大廳)
═══════════════════════════════════════════════════════════════
  測試:119 過 / 0 失敗(physics 50 真 Rapier / rules 47 假 renderer / smoke 22 含兩道靜態守門)
  真瀏覽器:全套 52 過 / 0 失敗、快檢 30 過 / 0 失敗(npm run verify / verify:quick,截圖存 .shots/ 不入庫;
            本機與線上同一支跑,連跑兩輪一致)
  建置:npm run build 過。index 510 KB(gzip 133)+ rapier.es 2.06 MB(gzip 762 KB)
  上線:CF Pages 專案 hfpc-animalbrawl3d → https://hfpc-animalbrawl3d.pages.dev
  大廳:總入口「經典街機合輯」第 5 張卡(hfpc-bible-games 的 src/data.js,新 kind brawl)
  統計:三層打點都鋪好(開啟 / -done 完賽 / -dwell 真實停留)
  版本號:選單底 v1.3 + 新增功能簡歷(game-must-haves ⑦)
  手機:直向蓋版「請轉橫向」提示(純 CSS media query)
  返回大廳:左上固定鈕,退出不走 onComplete(不計分),比賽中才輕量確認

═══════════════════════════════════════════════════════════════
★ 0901 真瀏覽器驗收抓到的兩個 bug(headless 抓不到,都已修)
═══════════════════════════════════════════════════════════════
  ① HUD 整組不顯示(比分卡/回合訊息/左下視角提示全都看不到)
  ② 比賽結束後「再來一場」鈕不出現(高 0px)⇒ 平板沒鍵盤按不到 R = 一場打完就卡死

  兩個同一個病根:元素在 CSS 裡是 display:none,而 JS 把 display 指派成**空字串**
  想「恢復顯示」—— 清掉 inline 樣式之後回落到 CSS 的 none,永遠看不見。

  ⚠ 為什麼 119 項測試全綠還是漏掉:測試讀的是 game.hud() 那個**物件**,
    而那個物件永遠是對的;錯的是「畫面上看不看得見」。
    連 getComputedStyle 拿字級都拿得到值(祖先 display:none 不影響 font-size)。
    ⇒ **驗「資料對不對」永遠驗不到「看不看得見」。**
  ⇒ 已補靜態守門(smoke 最後一段)+ 驗收腳本改成量 getBoundingClientRect / computed display。

═══════════════════════════════════════════════════════════════
★ 調參數之前先看這條(整個建構過程最值錢的一條)
═══════════════════════════════════════════════════════════════
  脖子那一關:我連續做了四次「調參數 / 再加一層力」——
  加力矩(只改朝向、改不了位置)→ 加位置彈簧(注入動量,身體被推著漂 2.69 m)
  → 補反作用力 → 6 組參數掃描,**每一組都有一項紅**。
  真兇是結構:Rapier 的球窩關節**沒有角度限位**,頭可以盪到胸口旁邊,
  而那個姿勢完全滿足關節約束(所以不會有任何錯誤)。
  改成帶限位的 revolute 之後**一次修掉兩項紅**。
  ⇒ **同一個地方調第三次還過不了,就該懷疑結構,不要繼續轉旋鈕。**
     「一改就修掉兩項」是找到真因的特徵;「改 A 紅 B、改 B 紅 A」是還在繞。

═══════════════════════════════════════════════════════════════
★ 0901 另一條同等值錢的:紅燈先問「是不是我的驗法」
═══════════════════════════════════════════════════════════════
  瀏覽器驗收第一輪 12 紅,**其中 6 條是假的** —— 而且假紅長得跟真 bug 一模一樣
  (「跳不起來」「打不到人」「框不住兩隻」),我差點去改產品程式。
  真兇兩個,都在驗收腳本自己身上:
    ・台子半徑 3.6,而我每個視角按 D 走 1.5~1.8 m ⇒ **驗收自己把角色走下台了**
      (髖部 y = -245 公尺,之後每一項當然都紅)
    ・「還活著」的門檻寫 y > -1,而**站姿髖部是 0.64** ⇒ 掉到 -0.58 還被判活著
  ⇒ 門檻要對著**真實的量值**訂,不要對著「還沒掉到很遠」訂。
  ⇒ 會轉的鏡頭(跟隨視角)要**逐幀**比對位移與鏡頭方向,不能動完才取一次方向
    (同一段碼第一輪 +1.00、第二輪 -0.97,不是遊戲壞了,是量法會翻號)。

═══════════════════════════════════════════════════════════════
★ 0901 下午改了什麼(使用者點名的三件觀感)
═══════════════════════════════════════════════════════════════
  ① 固定機位「上半部一大片空天空、角色偏小」
     ⇒ 拉近 + 加大俯角 + 看的點下移。角色佔畫面高 13% → 15~19%。
  ② 「文件說有雲、畫面上一朵都沒有」
     真因三層疊:材質吃霧而**霧色跟天空同色**、擺在 y=7~19(俯角看不到)、散太遠。
     ⇒ fog:false + 大部分移到**台子下方**(浮空台本來就該看到腳下的雲)。框內 0 → 11~13 朵。
  ③ 跟隨視角太低太近、常看不到對手 ⇒ 拉高拉遠並跟著兩隻的間距一起退。
  順帶:高空俯瞰改成**固定在場地中心正上方**(原本跟著玩家平移,玩家一偏台緣就出框,
        而那個視角的職責正是「看到整個台子」)。

  ★ 每一項都留了機器可驗的斷言(角色佔畫面高幾 %、框內幾朵雲、兩隻是否都在框內),
    不是「我看起來覺得好一點」。門檻按視角職責分級:固定機位 14%、替代機位 12%、
    俯瞰不套字級門檻(改驗「整個台子框得住」)。

■ ⏩ 進行中:無。
■ ➡ 下一件:roadmap 第 1 列 = 3~4 人同場(物理層的 addAnimal 本來就吃 N 隻,
   要改的是規則層誰得分、HUD 版面、出生點分佈、鏡頭要框得住所有人)。
